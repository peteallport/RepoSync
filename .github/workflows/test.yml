name: Test

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  safety:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v6
      - name: Run safety and installation tests
        run: ./test/run.sh
